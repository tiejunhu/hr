<div class="btn-toolbar">
<div class="btn-group">
	<%= link_to 'New', "#", :id => "new_human_link", :class => "btn" %>
	<%= link_to 'Edit', "#", :id => "edit_human_link", :class => "btn" %> 
	<%= link_to 'Del', "#", :id => "delete_human_link", :confirm => 'Are you sure?', :method => :delete, :remote => true, :class => "btn" %>
</div>
</div>
<table class="table table-bordered" id="table">
	<thead>
		<tr>
			<th>ID</th>
			<th>Name</th>
			<th>Login</th>
			<th>Gender</th>
			<th>Marriage</th>
			<th>Dept</th>
			<th>Board date</th>
			<th>State</th>
		</tr>
	</thead>
	<tbody>
	</tbody>
</table>

<% content_for :header do %>
	<%= javascript_include_tag humen_path %>
<% end %>